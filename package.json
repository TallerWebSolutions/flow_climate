{
  "name": "flow_climate",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "husky": "^7.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "heroku-prebuild": "cd app/spa && npm install && npm run build",
    "start": "cd app/spa && npm start"
  }
}
