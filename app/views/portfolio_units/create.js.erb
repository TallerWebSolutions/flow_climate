<% if flash[:error].present? %>
  $('#portfolio-units-table').html("<%= escape_javascript(render('portfolio_units/new', company: @company, product: @product, portfolio_unit: @portfolio_unit)) %>")
<% else %>
  $('#portfolio-units-table').html("<%= escape_javascript(render('portfolio_units/portfolio_units_table', company: @company, product: @product, portfolio_units: @portfolio_units)) %>")
<% end %>