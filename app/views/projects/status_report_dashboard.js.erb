var statusReportDiv = $("#status-report-div").modal({
    show: false
});

statusReportDiv.html("<%= escape_javascript(render 'projects/dashboards/status_report_dashboard', project: @project, project_summary: @project_summary) %>");

statusReportDiv.modal("show");

buildStatusReportDashboardHighcharts();
