var statusReportDiv = $("#status-report-div").modal({
    show: false,
    backdrop: 'static',
    keyboard: false
});

statusReportDiv.html("<%= escape_javascript(render 'projects/dashboards/status_report_dashboard', project: @project, project_summary: @project_summary, work_item_flow_information: @work_item_flow_information, x_axis: @x_axis, average_speed: @average_speed) %>");

statusReportDiv.modal("show");

buildStatusReportDashboardHighcharts();
