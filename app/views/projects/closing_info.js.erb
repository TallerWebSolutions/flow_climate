var closingInfoDiv = $("#closing-info-div").modal({
    show: false,
    backdrop: 'static',
    keyboard: false
});

closingInfoDiv.html("<%= escape_javascript(render 'projects/dashboards/closing_dashboard', project: @project, project_summary: @project_summary, average_speed: @average_speed) %>");

closingInfoDiv.modal("show");

buildClosingDashboardHighcharts();
