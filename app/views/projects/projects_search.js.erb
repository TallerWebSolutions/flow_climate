$("#projects-table").html("<%= escape_javascript(render 'projects/projects_table', company: @company, start_date: @start_date, end_date: @end_date, projects_list: @searched_projects, projects_summary: @projects_summary, projects_ids: @projects.map(&:id).flatten.join(',')).html_safe %>");

$(".col-table-details").hide();
