var projectsDiv = $('#projects-table')

projectsDiv.html("<%= escape_javascript(render 'projects/projects_table', company: @company, start_date: @start_date, end_date: @end_date, projects_list: @product_projects, projects_summary: @projects_summary, projects_ids: @product_projects.map(&:id).flatten.join(',')) %>")

$('#general-loader').hide();

projectsDiv.show();

enableTabs();
$(".col-table-details").hide();
