var projectsDiv = $('#projects-table')

projectsDiv.html("<%= escape_javascript(render 'projects/projects_table', company: @company, target_name: @target_name, projects_list: @projects, projects_summary: @projects_summary) %>")

$('#general-loader').hide();

projectsDiv.show();

enableTabs();

$(".col-table-details").hide();
bindSearchProjects();
