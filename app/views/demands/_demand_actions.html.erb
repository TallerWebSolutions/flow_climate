<% unless demand.discarded? %>
  <%= link_to '#', onclick: "destroyDemand(#{company.id}, #{demand.id}, '#{I18n.t('general.destroy.confirm')}', #{demands_ids})" do %>
    <i class="fa fa-times" aria-hidden="true"></i>
  <% end %>
<% end %>

<%= link_to '#', class: 'edit-demand-button', onclick: "editDemand(#{company.id}, #{demand.project_id}, #{demand.id})" do %>
  <i class="fas fa-edit"></i>
<% end %>

<% if demand.external_url.present? %>
  <%= link_to demand.external_url, target: :_blank do %>
    <i class="fas fa-external-link-alt"></i>
  <% end %>
<% end %>

<%= link_to score_research_company_demand_path(company, demand) do %>
  <i class="fas fa-tasks"></i>
<% end %>
