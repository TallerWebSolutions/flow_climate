var demandsBlocksDiv = $('#demands-blocks-tab')

demandsBlocksDiv.html("<%= escape_javascript(render 'demand_blocks/demands_blocks_tab', company: @company, demands_blocks: @demands_blocks, projects_ids: @projects_ids) %>");

$('#general-loader').hide();
demandsBlocksDiv.show();

enableTabs();
