var updatedRow = $('#block_row_<%= @demand_block.id %>');
updatedRow.html("<%= escape_javascript(render 'demand_blocks/demand_block_fields', block: @demand_block).html_safe %>");

$("#edit-block-form").modal('toggle');
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();

updatedRow.effect("highlight", { color: "lightgreen" }, 2000);
