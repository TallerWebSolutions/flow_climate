var updatedRow = $('#stage_row_<%= @stage.id %>');
updatedRow.html("<%= escape_javascript(render 'stages/stage_fields', stage: @stage).html_safe %>");

$("#edit-stage-form").modal('toggle');
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();

updatedRow.effect("highlight", { color: "lightgreen" }, 2000);
