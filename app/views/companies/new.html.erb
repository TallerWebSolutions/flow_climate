<div class="row">
  <div class="col-xs-12">
    <ul class="breadcrumb">
      <li><%= link_to I18n.t('general.home.title'), root_path %></li>
      <li><%= I18n.t('companies.new.title') %></li>
    </ul>
  </div>
</div>

<%= form_for @company do |f| %>
  <div class="row bottom-spaced-component">
    <div class="col-xs-12">
      <%= render 'form', f: f, company: @company %>
    </div>
  </div>
<% end %>
