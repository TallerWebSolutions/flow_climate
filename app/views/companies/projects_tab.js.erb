var projectsTableDiv = $('#projects-table')

projectsTableDiv.html("<%= escape_javascript(render 'projects/projects_table', company: @company, target_name: @company.name, projects_list: @projects, projects_summary: @projects_summary).html_safe %>");

$('#general-loader').hide();
$('#project-list').show();

enableTabs();

$(".col-table-details").hide();

projectsTableTabBehaviour();
