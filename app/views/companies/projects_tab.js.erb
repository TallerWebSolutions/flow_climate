var projectsTableDiv = $('#projects-table')

projectsTableDiv.html("<%= escape_javascript(render 'projects/projects_table', projects_list: @company_projects).html_safe %>");

$('#general-loader').hide();
$('#project-list').show();

enableTabs();
