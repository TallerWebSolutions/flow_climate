<div class="row">
  <div class="col-xs-12">
    <ul class="breadcrumb">
      <li><%= link_to t('general.home.title'), root_path %></li>
      <li><%= link_to @company.abbreviation&.upcase, company_path(@company) %></li>
      <li><%= link_to @team.name, company_team_path(@company, @team) %></li>
      <li><%= t('pipefy_team_config.edit.title') %></li>
    </ul>
  </div>
</div>

<%= form_for @pipefy_team_config, url: company_team_pipefy_team_config_path(@company, @team, @pipefy_team_config), method: :put, class: 'form' do |f| %>
  <div class="row bottom-spaced-component">
    <div class="col-xs-12">
      <%= render 'form', f: f %>
    </div>
  </div>
<% end %>
